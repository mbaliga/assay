# assay android-console — installable test build

Not for distribution. Debug/test build, signed with the standard Android **debug key**, so it
will not update over a Play-signed install.

**Direct download:** [`assay.apk`](https://github.com/mbaliga/assay/raw/apk-testbuild/assay.apk)

Built from `30e45fb` on branch
`claude/repo-status-overview-2c0rgm`.

This is an **orphan branch** with no shared history — it exists only to serve a downloadable file.
Delete it with `git push origin --delete apk-testbuild` when it is no longer needed.
